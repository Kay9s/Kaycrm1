-- EXACT CONFIGURATION FOR YOUR N8N POSTGRESQL NODE --

1. In your PostgreSQL node, replace the Query with this EXACT text:

INSERT INTO bookings (
  customer_name,
  customer_phone,
  vehicle_make,
  vehicle_model,
  start_date,
  end_date,
  special_requests,
  booking_ref,
  status,
  source
) 
VALUES (
  $1, $2, $3, $4, $5, $6, $7, $8, 'confirmed', 'n8n_voice'
)
RETURNING 
  id,
  booking_ref,
  customer_name,
  customer_phone,
  vehicle_make,
  vehicle_model,
  start_date,
  end_date,
  status

2. In the Parameters section, add these 8 parameters exactly:

Parameter 1: {{ $json.full_name }}
Parameter 2: {{ $json.phone }}
Parameter 3: {{ $json.vehicle_make }}
Parameter 4: {{ $json.vehicle_model }}
Parameter 5: {{ $json.start_date }}
Parameter 6: {{ $json.end_date }}
Parameter 7: {{ $json.special_requests }}
Parameter 8: {{ $json.booking_id }}

3. Make sure "Consider using query parameters" is ENABLED
4. Set Operation to "Execute Query"